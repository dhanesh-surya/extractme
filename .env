# Django Settings
SECRET_KEY=django-insecure-marksheet-ocr-app-2026-change-in-production
DEBUG=True

# Google Gemini API
GEMINI_API_KEY=your-gemini-api-key-here

# Database (for local development, use SQLite by leaving DATABASE_URL empty)
# For production on Render, this will be auto-set by Render
# DATABASE_URL=

# Allowed Hosts (comma-separated, no spaces)
ALLOWED_HOSTS=localhost,127.0.0.1,*

# Render-specific (set automatically on Render.com)
# RENDER=
# RENDER_EXTERNAL_HOSTNAME=
